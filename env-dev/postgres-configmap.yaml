apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-db
  namespace: dev
  labels:
    name: postgres-db
    type: back-end
    app: user-service
data:
  POSTGRES_DB: user-service-db
  POSTGRES_USER: developer-app
  POSTGRES_PASSWORD: strong_password
